我先查看第 1 章示例代码，确认第 4 题所说的 Thought-Action-Observation 循环及其接口，再据此给出完整、可落地的作答。

Read , lines 1 to 260
Completed with input: {
  "path": "D:\\hello-agents-main\\code\\chapter1\\FirstAgentTest.py",
  "view_range": [
    1,
    260
  ]
}

Find files matching `**/*chapter1*`

# 习题参考答案

## 1. 四个 Case 中的主体是否属于智能体

判断一个系统是否属于智能体，通常要看它是否能够：

1. 感知环境；
2. 根据目标进行决策；
3. 采取行动影响环境；
4. 在一定程度上自主运行。

### Case A：冯·诺依曼结构的超级计算机

**通常不单独认为它是智能体。**

它虽然具有极强的计算能力，但“算力强”并不等于“具备智能体能力”。题目只说明它是一台超级计算机，没有说明它是否能够感知外部环境、设定目标、进行自主决策或执行动作。

从分类角度看：

- 它首先是一个**计算系统**；
- 可以作为智能体的基础设施或执行平台；
- 如果在其上运行了具备感知、决策和行动能力的软件系统，那么该软件系统才可能是智能体。

例如，同一台超级计算机可以：

- 运行气象模拟程序：通常只是计算程序；
- 运行自动交易系统：可能属于智能体；
- 运行机器人控制系统：可能属于智能体。

因此，**硬件本身不是智能体，运行于其上的自主软件系统才可能是智能体。**

---

### Case B：特斯拉自动驾驶系统

**属于智能体。**

它能够：

- 通过摄像头、雷达等传感器感知道路环境；
- 判断障碍物、车辆、车道和交通状况；
- 结合行驶目标进行决策；
- 通过制动、转向和加速执行动作。

从不同维度看，它可以被描述为：

- **反射式智能体**：遇到障碍物时需要快速制动或变道；
- **基于模型的智能体**：需要维护对道路、车辆和障碍物状态的内部估计；
- **目标导向智能体**：目标是安全到达目的地；
- **效用型智能体**：在安全性、舒适性、通行效率等因素之间进行权衡；
- **实时智能体**：必须在毫秒级或较短时间内作出响应；
- **部分可观察智能体**：传感器无法完全获取环境信息；
- **动态智能体**：道路环境和其他车辆状态会持续变化；
- **多智能体环境中的智能体**：道路上还有其他车辆、行人等具有独立行为的主体。

它的核心难点不是单纯识别障碍物，而是在不确定信息下选择风险最小的行动。

---

### Case C：AlphaGo

**属于智能体。**

AlphaGo 能够：

- 感知当前棋盘状态；
- 评估不同落子的结果；
- 预测未来局面；
- 搜索和规划可能的行动序列；
- 选择并执行落子动作。

从分类角度看，它主要是：

- **目标导向智能体**：目标是赢得棋局；
- **效用型智能体**：选择能够最大化获胜概率的落子；
- **基于模型的智能体**：内部维护棋局状态和对未来局面的预测；
- **规划型智能体**：会评估未来数十步甚至更多步骤；
- **学习型智能体**：通过监督学习、自我博弈和强化学习不断提升能力；
- **离散环境中的智能体**：行动和状态主要是离散的；
- **确定性环境中的智能体**：规则本身确定，但对手行为具有不确定性；
- **对抗性多智能体系统**：对手是另一个具有目标的决策主体。

与自动驾驶相比，AlphaGo 的实时性要求相对较低，但规划深度和策略推理要求更高。

---

### Case D：ChatGPT 扮演的智能客服

**在具备工具调用、状态维护和任务执行能力时，属于智能体或智能体系统。**

它能够：

- 理解用户投诉；
- 查询订单、物流和售后信息；
- 分析问题原因；
- 根据政策生成解决方案；
- 必要时转交人工；
- 通过语言交互安抚用户。

从分类角度看，它属于：

- **知识型智能体**：依赖企业政策、订单知识和业务规则；
- **目标导向智能体**：目标是解决用户问题并完成服务流程；
- **工具使用型智能体**：可以调用订单查询、退款、工单等工具；
- **对话型智能体**：通过自然语言与用户交互；
- **部分可观察智能体**：无法完全知道用户真实意图和情绪；
- **学习型或适应型智能体**：可以根据用户偏好、历史对话和反馈调整策略；
- **混合型智能体**：通常同时包含语言模型、规则引擎、检索系统和业务工作流。

需要注意：如果 ChatGPT 只是生成一段客服话术，而不能查询数据、执行操作或跟踪任务，那么它更接近**对话模型**，而不是完整的智能体。

---

## 2. 智能健身教练的 PEAS 描述

PEAS 分别表示：

- **P：Performance measure，性能指标**
- **E：Environment，任务环境**
- **A：Actuators，执行器**
- **S：Sensors，传感器**

### P：性能指标

可以包括：

1. **训练目标达成度**
   - 是否帮助用户减脂、增肌或提升耐力；
   - 训练计划完成率；
   - 用户体能指标改善情况。

2. **训练安全性**
   - 是否避免心率过高；
   - 是否及时识别疲劳、头晕、过度训练等风险；
   - 是否降低运动损伤概率。

3. **训练计划质量**
   - 训练强度是否合适；
   - 计划是否符合用户当前状态；
   - 是否能够根据训练效果动态调整。

4. **动作指导质量**
   - 动作识别准确率；
   - 错误动作纠正及时性；
   - 指导建议的可执行性。

5. **用户体验**
   - 用户满意度；
   - 用户坚持率；
   - 用户对语音指导的接受程度；
   - 用户投诉率。

6. **个性化程度**
   - 是否符合用户目标、时间、偏好和身体条件；
   - 是否能够适应用户近期状态变化。

7. **饮食建议的合理性**
   - 建议是否满足营养需求；
   - 是否符合用户的饮食偏好和健康限制；
   - 对过敏、慢性病等情况是否足够谨慎。

---

### E：任务环境

环境包括：

- 用户本人；
- 健身房、家庭、户外等运动场景；
- 跑步机、哑铃、杠铃、健身车等器械；
- 用户的运动目标；
- 用户历史训练记录；
- 用户的健康状况；
- 用户的睡眠、饮食和压力状态；
- 周围噪声、光线和网络状况；
- 训练过程中的突发情况；
- 医疗或健康风险因素。

---

### A：执行器

智能教练可以通过以下方式影响环境：

- 语音播报训练指令；
- 提示开始、暂停、加速或降低强度；
- 提醒热身和拉伸；
- 实时纠正动作；
- 调整训练计划；
- 推荐训练项目；
- 提醒补水和休息；
- 提供饮食建议；
- 发送异常警告；
- 在严重情况下建议停止训练并联系医生或急救人员；
- 将训练报告推送给用户或授权教练。

---

### S：传感器

可以使用：

#### 可穿戴设备

- 心率；
- 心率变异性；
- 步数；
- 卡路里消耗；
- 血氧；
- 运动时长；
- 加速度和陀螺仪数据；
- GPS 位置信息。

#### 摄像头或动作传感器

- 姿态；
- 关节角度；
- 动作轨迹；
- 重复次数；
- 动作速度；
- 左右侧发力是否平衡。

#### 用户输入

- 健身目标；
- 年龄、身高、体重；
- 既往病史；
- 运动经验；
- 时间安排；
- 饮食偏好；
- 疼痛和疲劳反馈。

#### 外部数据

- 天气；
- 空气质量；
- 日历和时间安排；
- 健身器械状态；
- 用户授权的健康档案。

---

### 该环境的主要特性

#### 1. 部分可观察

是。

智能教练无法直接观察用户全部状态。例如：

- 用户是否感到疼痛；
- 用户是否即将晕厥；
- 用户是否故意隐瞒病史；
- 用户的主观疲劳程度；
- 用户动作内部的肌肉发力情况。

因此需要结合传感器数据、用户反馈和历史记录进行推断。

#### 2. 随机性

具有随机性。

例如：

- 心率会受到睡眠、情绪、咖啡因等因素影响；
- 用户的动作可能突然失误；
- 传感器可能出现噪声；
- 网络连接可能中断；
- 用户可能临时改变训练计划。

相同的训练指令不一定产生相同的结果。

#### 3. 动态性

是动态环境。

用户的状态会随着训练不断变化，心率、疲劳和动作质量都会实时改变。因此，智能体不能只在训练开始时制定一次计划，而需要持续感知和调整。

#### 4. 连续性

主要是连续环境。

例如：

- 心率是连续变化的；
- 运动速度和力量是连续变化的；
- 动作角度和身体姿态也是连续变量。

但训练项目、是否休息、是否结束训练等决策又具有离散性，所以它是连续与离散混合的环境。

#### 5. 序贯性

是序贯环境。

当前的训练强度会影响之后的疲劳程度，当前动作质量会影响后续训练效果。每一步决策都会对未来状态产生影响。

#### 6. 多智能体特性

通常不是强多智能体环境，但可能存在人与智能教练之间的交互。用户并不是完全被动执行者，用户会拒绝建议、改变目标或中断训练，因此智能教练需要具备协商和适应能力。

#### 7. 高风险特性

在涉及心血管疾病、运动损伤或严重疲劳时，系统属于安全敏感场景。智能体不应只追求训练效果，还必须设置安全约束，并在必要时交由专业医生或人工教练处理。

---

## 3. Workflow 与 Agent 退款方案比较

## 方案 A：Workflow

### 优点

1. **规则清晰**
   - 每个金额、时间和商品类型都有明确处理方式。

2. **可解释性强**
   - 可以直接说明“因为超过 7 天”或“因为属于定制品”。

3. **稳定性高**
   - 相同输入通常得到相同输出。

4. **容易审计**
   - 便于检查是否符合公司政策和监管要求。

5. **实现成本低**
   - 不需要复杂模型、训练数据或大语言模型。

6. **速度快**
   - 对简单规则可以立即给出结果。

7. **适合高确定性业务**
   - 退款政策本身往往包含大量明确规则。

### 缺点

1. **灵活性不足**
   - 难以处理规则之外的复杂情况。

2. **难以理解自然语言**
   - 用户的投诉可能需要语义理解和上下文分析。

3. **难以利用历史信息**
   - 例如用户过去是否频繁恶意退款。

4. **规则维护成本可能较高**
   - 政策变化时需要修改大量分支逻辑。

5. **用户体验较机械**
   - 不能很好地理解用户情绪或进行协商。

---

## 方案 B：Agent

### 优点

1. **能够理解复杂语义**
   - 可以处理用户对商品质量、物流、售后过程的自然语言描述。

2. **能够综合多种信息**
   - 结合订单历史、商品信息、物流记录、图片和用户行为进行判断。

3. **具有较强灵活性**
   - 能够处理规则未明确覆盖的边界情况。

4. **交互体验更自然**
   - 可以解释政策、询问补充信息并安抚用户。

5. **能够调用多个工具**
   - 查询订单、物流、库存、优惠券、售后记录等。

6. **可以发现异常模式**
   - 例如识别批量退款、重复索赔或疑似欺诈行为。

### 缺点

1. **可能产生幻觉**
   - 可能错误理解退款政策或虚构订单信息。

2. **结果稳定性较差**
   - 相似案例可能出现不同判断。

3. **可解释性和可审计性较弱**
   - 特别是涉及多个模型推理步骤时。

4. **存在越权风险**
   - 如果工具权限设计不当，可能错误批准退款或泄露信息。

5. **成本更高**
   - 涉及模型调用、工具系统、日志、监控和评测。

6. **容易被用户操纵**
   - 用户可能通过提示注入或夸大事实影响智能体判断。

7. **不适合完全自动处理高风险决策**
   - 大额退款、法律争议和疑似欺诈应保留人工审核。

---

## 什么时候 Workflow 更合适

Workflow 更适合：

- 退款规则明确；
- 输入信息结构化；
- 业务量很大；
- 需要稳定、可预测的结果；
- 合规和审计要求较高；
- 决策风险较高；
- 不需要大量自然语言理解；
- 例如“7 天内、普通商品、金额低于 100 元”的自动审批。

---

## 什么时候 Agent 更有优势

Agent 更适合：

- 用户描述复杂且非结构化；
- 需要综合多种信息；
- 政策存在大量例外；
- 需要多轮沟通；
- 需要分析图片、聊天记录或物流信息；
- 需要识别用户意图和情绪；
- 需要根据个体情况提供解释和建议。

---

## 负责人更倾向于哪种方案

实际业务中不建议简单地二选一。

如果必须先选择，我会优先采用：

> **以 Workflow 为核心、Agent 作为辅助的混合方案。**

原因是退款审批涉及资金和合规，核心决策应当稳定、可解释、可审计。Agent 可以负责：

- 理解用户问题；
- 抽取订单和商品信息；
- 总结争议原因；
- 查询相关记录；
- 推荐适用的政策条款；
- 为客服生成回复；
- 对复杂案例进行风险分级。

但最终是否自动批准，应由规则和权限系统控制。

---

## 方案 C：Workflow + Agent 混合架构

可以设计为：

```text
用户请求
   ↓
Agent 理解问题并抽取结构化信息
   ↓
政策规则引擎进行硬约束判断
   ↓
风险评估模型或 Agent 进行补充分析
   ↓
低风险且满足规则 → 自动批准
边界情况 → 客服审核
高金额、高风险或疑似欺诈 → 主管审批
   ↓
Agent 生成解释和客服回复
```

### 方案 C 的关键原则

1. **规则优先**
   - Agent 不能绕过退款政策。

2. **权限隔离**
   - Agent 只能调用被授权的工具；
   - 不同金额对应不同审批权限。

3. **高风险人工兜底**
   - 大额退款、定制品、异常用户和争议案件转人工。

4. **强制结构化输出**
   - Agent 输出订单号、政策条款、风险等级、建议动作等字段，而不是直接拥有最终权限。

5. **完整日志**
   - 记录输入、模型判断、规则命中、工具调用和最终审批人。

6. **置信度和不确定性控制**
   - 低置信度时不能自动通过，应转人工审核。

---

## 4. 为智能旅行助手增加功能

现有示例的核心循环是：

```text
Thought → Action → Observation → Thought → Action ...
```

代码位于 `FirstAgentTest.py`，目前主要支持天气查询和景点搜索。

---

### 4.1 增加用户记忆功能

可以建立一个用户画像或记忆存储，例如：

```python
user_memory = {
    "preferences": {
        "attraction_types": ["历史文化"],
        "budget": "100-300元",
        "transport": "公共交通"
    },
    "history": []
}
```

当用户说：

```text
我喜欢历史文化景点，预算每天 300 元以内。
```

智能体可以执行：

```text
Thought: 用户表达了长期偏好，需要更新用户记忆。
Action: MemoryUpdate(preference="喜欢历史文化景点，预算每天300元以内")
```

之后在推荐时读取记忆：

```text
Thought: 我需要结合用户过去的历史文化偏好和预算进行推荐。
Action: get_attraction(
    city="北京",
    weather="晴天",
    preferences="历史文化",
    budget="300元以内"
)
```

### 记忆功能应区分两类

#### 长期记忆

例如：

- 喜欢历史文化景点；
- 不喜欢拥挤场所；
- 预算范围；
- 喜欢公共交通；
- 对某类食物过敏。

#### 短期记忆

例如：

- 本次旅行的日期；
- 当前天气；
- 本轮已经推荐过的景点；
- 用户刚刚拒绝的推荐。

不应把所有对话都永久保存，应该只保存经过筛选的、有用且得到授权的信息。

---

### 4.2 门票售罄时推荐备选方案

可以增加工具：

```python
def check_ticket(attraction: str, date: str) -> str:
    ...

def get_alternatives(
    city: str,
    attraction: str,
    weather: str,
    preferences: str
) -> str:
    ...
```

循环过程可以变为：

```text
Thought: 用户想去故宫，我先查询门票状态。
Action: check_ticket(attraction="故宫", date="2026-09-16")
```

```text
Observation: 故宫当日门票已售罄。
```

```text
Thought: 目标景点不可用，我需要寻找满足相似偏好的备选景点。
Action: get_alternatives(
    city="北京",
    attraction="故宫",
    weather="晴天",
    preferences="历史文化"
)
```

备选景点最好满足以下条件：

- 类型相似；
- 距离合理；
- 预算接近；
- 与天气匹配；
- 时间上可安排；
- 门票仍有库存；
- 没有重复推荐。

同时需要明确告诉用户：

> 原推荐景点门票已售罄，因此推荐以下替代方案。

不能把“可能有票”说成“确定有票”，库存信息应尽量来自实时查询。

---

### 4.3 连续拒绝 3 个推荐后的反思机制

需要在状态中记录：

```python
state = {
    "rejected_recommendations": [],
    "rejection_count": 0,
    "strategy": {
        "attraction_type": "历史文化",
        "budget": "中等",
        "distance": "市区"
    }
}
```

每次用户拒绝推荐时：

```python
state["rejection_count"] += 1
state["rejected_recommendations"].append(attraction)
```

达到 3 次后，不应该继续机械推荐，而应触发反思：

```text
Thought: 用户已经连续拒绝了三次推荐，说明当前推荐策略可能不符合真实偏好。
Action: Reflect(
    rejected_items=[景点A, 景点B, 景点C],
    reason="连续拒绝三次"
)
```

反思结果可以包括：

- 原先理解的“历史文化”过于宽泛；
- 用户可能更重视安静、免费或交通便利；
- 用户可能不喜欢热门景点；
- 用户可能希望推荐餐厅、展览或室内活动；
- 当前预算或距离估计不准确。

然后主动询问一个澄清问题，例如：

> 你更在意安静、人少、免费，还是交通方便？

或者给出类别选项，而不是继续盲目推荐。

新的循环可以是：

```text
用户请求
  ↓
读取记忆
  ↓
生成推荐
  ↓
检查门票和可用性
  ↓
推荐或提供备选
  ↓
记录用户接受/拒绝
  ↓
连续拒绝达到阈值
  ↓
反思并请求澄清
  ↓
更新用户模型
  ↓
生成新策略
```

### 一个简化的伪代码

```python
for i in range(MAX_STEPS):
    response = llm.generate(build_prompt(history, memory, state))

    action = parse_action(response)

    if action.type == "finish":
        return action.answer

    observation = execute_action(action)

    if action.type == "recommendation":
        if user_rejected(observation):
            state["rejection_count"] += 1
            state["rejected_recommendations"].append(action.item)

            if state["rejection_count"] >= 3:
                state["reflection_required"] = True

        history.append(observation)
```

实际实现时还需要专门解析用户后续反馈，而不是只执行一次固定的用户请求。

---

## 5. 系统 1 与系统 2 的智能体协同

### 场景：医疗诊断助手

该系统辅助医生分析患者症状、检查报告和病史，但不直接替代医生作出最终诊断。

---

### 系统 1：快速直觉处理的任务

系统 1 适合处理速度要求高、模式比较明显的任务，例如：

1. 从病历中提取症状；
2. 识别医学实体；
3. 读取和归类检验指标；
4. 识别明显异常值；
5. 对医学影像进行初步筛查；
6. 根据症状匹配常见疾病模式；
7. 检索可能相关的指南和病例；
8. 判断是否需要紧急处理；
9. 对患者问题进行初步分流；
10. 生成医生可以快速查看的摘要。

例如：

```text
患者胸痛、出汗、心电图 ST 段异常
→ 系统 1 快速标记为“疑似急性心血管事件，需要优先处理”
```

系统 1 的特点是：

- 速度快；
- 适合大量数据处理；
- 擅长模式识别；
- 但容易受相似症状、异常数据和分布变化影响。

---

### 系统 2：慢速推理处理的任务

系统 2 适合处理高风险、复杂、需要解释的任务，例如：

1. 构建鉴别诊断列表；
2. 分析多个疾病之间的差异；
3. 综合病史、检查和用药情况；
4. 判断异常指标之间是否具有因果关联；
5. 处理罕见病或互相矛盾的证据；
6. 检查诊断是否违反临床指南；
7. 评估误诊和漏诊风险；
8. 设计下一步检查方案；
9. 解释为什么推荐某种诊断；
10. 判断是否必须交由医生复核。

例如：

```text
系统 1 判断“可能是肺炎”，
系统 2 进一步检查：
- 是否存在心衰；
- 是否有肺栓塞风险；
- 是否有药物副作用；
- 影像和炎症指标是否一致；
- 是否需要进一步检查。
```

---

### 两个系统如何协同

可以采用以下流程：

```text
患者输入
   ↓
系统 1 快速抽取和筛查
   ↓
识别是否存在高风险或不确定性
   ↓
简单、低风险情况 → 系统 1 给出辅助建议
复杂、高风险或冲突情况 → 系统 2 深度分析
   ↓
系统 2 检查证据、规则和推理过程
   ↓
输出候选诊断、依据、不确定性和下一步建议
   ↓
医生最终确认
```

### 协同原则

1. **系统 1 负责召回**
   - 尽量不要漏掉潜在危险情况。

2. **系统 2 负责审查**
   - 对高风险结果进行更严格分析。

3. **规则系统提供硬约束**
   - 例如药物禁忌、生命体征阈值和临床指南。

4. **医生拥有最终决策权**
   - 医疗领域不能把不透明的模型输出直接当作最终诊断。

5. **保留不确定性**
   - 系统应明确说明“证据不足”“需要进一步检查”，而不是强行给出确定答案。

---

## 6. 智能体系统的局限与评估

## 6.1 为什么会产生幻觉

幻觉通常不是单一原因造成的，而是多个因素共同作用的结果。

### 1. 语言模型优化的是“可能的文本”，不是真实性

大语言模型本质上是在预测下一个最可能出现的词或句子。它并没有天然的事实验证机制，因此可能生成语言上流畅、逻辑上像真的、但实际错误的内容。

### 2. 训练数据存在缺失和噪声

训练数据可能：

- 不完整；
- 互相矛盾；
- 包含错误信息；
- 缺少最新信息；
- 覆盖不到小众领域。

模型会根据统计规律进行补全，而不是保证事实准确。

### 3. 信息不足时倾向于“补全”

当用户的问题缺少上下文时，模型可能推测用户真正想问什么，并直接给出答案，而不是承认“不知道”。

### 4. 检索或工具调用失败

智能体可能：

- 没有调用应该调用的工具；
- 调用错误的工具；
- 错误解析工具结果；
- 使用过期缓存；
- 误把错误信息当成有效信息。

### 5. 长上下文和状态管理错误

循环次数增加后，历史记录可能变得很长，导致：

- 关键信息被忽略；
- 旧信息覆盖新信息；
- 错误信息被重复强化；
- 行动和观察结果对应错位。

### 6. 工具输出本身存在问题

外部 API 可能返回：

- 空数据；
- 过期数据；
- 格式错误；
- 不完整结果；
- 权限错误。

如果系统没有对工具结果进行校验，智能体可能据此继续推理。

### 7. 缺少约束和验证

如果智能体可以随意生成最终答案，却没有：

- 事实核验；
- 结构化输出；
- 置信度判断；
- 规则校验；
- 人工审核；

那么幻觉更容易直接暴露给用户。

---

## 6.2 没有最大循环次数会有什么问题

第 1.3 节代码中使用了最大循环次数限制。这个限制非常重要。

如果没有限制，可能出现：

### 1. 无限循环

模型不断重复：

```text
Thought → Action → Observation → Thought → Action
```

但始终不执行 `Finish`。

### 2. 重复调用工具

例如反复查询同一个城市的天气，浪费：

- API 配额；
- 网络资源；
- 运行时间；
- 计算成本。

### 3. 错误不断自我强化

如果模型误解了某个 Observation，之后可能基于这个错误继续行动，形成错误循环。

### 4. Action 解析失败后反复重试

当前代码在无法解析 `Action` 时，会把错误 Observation 加入历史并继续循环。如果模型始终输出不符合格式的内容，就会一直重试。

### 5. 成本失控

每一轮通常都需要调用大语言模型，循环不受限制会导致费用和延迟不可控。

### 6. 状态膨胀

历史记录会不断增长，使 Prompt 越来越长，最终导致：

- 超过上下文长度；
- 调用失败；
- 推理质量下降；
- 成本大幅上升。

### 7. 工具副作用重复执行

如果工具不仅是查询，还包括下单、退款、发消息等操作，重复调用可能造成严重业务问题。

### 8. 用户体验恶化

用户可能长时间得不到结果，系统看起来像“卡死”或失控。

因此，最大循环次数是必要的，但不能只依赖它，还应配合：

- 超时控制；
- 工具调用次数限制；
- 重复行动检测；
- 状态机；
- 幂等机制；
- 错误次数上限；
- 最终兜底回复；
- 人工转接。

---

## 6.3 如何评估智能体的“智能”程度

**只使用准确率是不够的。**

准确率只能回答“结果对不对”，不能充分反映智能体是否：

- 能够理解任务；
- 能够合理规划；
- 能够处理不确定性；
- 能够安全地行动；
- 能够解释自己的决策；
- 能够在失败后恢复。

### 可以从以下维度进行评估

#### 1. 任务成功率

是否最终完成用户目标，例如：

- 是否成功查询天气；
- 是否推荐了符合条件的景点；
- 是否完成了订单售后流程。

#### 2. 事实准确性

生成的信息是否真实，工具结果是否被正确使用。

#### 3. 规划能力

是否能将复杂任务拆解成合理步骤，是否能选择合适的工具和顺序。

#### 4. 工具使用能力

包括：

- 工具选择是否正确；
- 参数是否正确；
- 是否能处理工具报错；
- 是否能理解工具返回结果；
- 是否避免重复调用。

#### 5. 鲁棒性

面对以下情况能否正常工作：

- 用户输入模糊；
- 工具暂时不可用；
- 数据不完整；
- 用户中途修改需求；
- 环境发生变化；
- 用户连续拒绝建议。

#### 6. 安全性

是否会：

- 泄露隐私；
- 执行越权操作；
- 绕过审批流程；
- 产生危险建议；
- 被提示注入攻击操纵。

#### 7. 可解释性

是否能说明：

- 使用了哪些信息；
- 为什么选择这个行动；
- 哪些内容来自工具；
- 哪些内容只是推测；
- 当前有哪些不确定性。

#### 8. 效率

包括：

- 完成任务所需轮数；
- 工具调用次数；
- 平均响应时间；
- Token 消耗；
- 运行成本。

#### 9. 交互质量

包括：

- 是否理解用户意图；
- 是否能够提出有效澄清问题；
- 是否记住用户偏好；
- 回复是否自然；
- 是否能处理用户情绪。

#### 10. 学习和适应能力

用户反馈后，智能体是否能够：

- 修正错误；
- 更新用户偏好；
- 调整策略；
- 避免重复犯错。

#### 11. 校准能力

模型的自信程度是否与实际正确率匹配。

例如：

- 高置信度答案是否通常正确；
- 不确定时是否会主动说明；
- 是否会在证据不足时请求更多信息。

---

### 一个更完整的评估指标体系

可以采用多维度评分：

```text
智能体综合表现 =
任务成功率
+ 事实准确性
+ 规划质量
+ 工具使用正确率
+ 鲁棒性
+ 安全性
+ 可解释性
+ 交互体验
+ 运行效率
+ 适应能力
```

不同应用的权重不同：

- 娱乐助手：交互体验和个性化可能更重要；
- 医疗助手：安全性和召回率更重要；
- 退款系统：合规性、可审计性和误批率更重要；
- 自动驾驶：安全性和实时性优先于语言表达能力。

因此，“智能”不是单一的准确率，而是系统在**复杂环境中持续、可靠、安全地实现目标的综合能力**。

---

## 额外的工程安全提醒

示例文件 `FirstAgentTest.py` 中包含直接写入源码的 API 密钥。无论这些密钥当前是否有效，都建议立即：

1. 撤销并重新生成相关密钥；
2. 从源码中删除密钥；
3. 改用环境变量或本地 `.env` 文件；
4. 将 `.env` 加入 `.gitignore`；
5. 检查 Git 历史和日志中是否已经泄露过这些密钥。

推荐使用：

```python
api_key = os.environ["OPENAI_API_KEY"]
tavily_api_key = os.environ["TAVILY_API_KEY"]
```

而不是把真实密钥直接写在 Python 文件中。